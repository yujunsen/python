import pygame

pygame.init()

#创建游戏窗口
screen =  pygame.display.set_mode((480, 700))
#set_mode(resolution=(0,0), flags=0, depth=0)
#resolution(x,y) x 宽度 高度 默认屏幕大小
#flags指定屏幕附加项 默认不需要
#depth 颜色位数 默认 自动匹配
while True:
    pass

pygame.quit()
